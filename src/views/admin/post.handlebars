<h3>Post Contents</h3>
<article id="post-container">
{{#if post.content}}{{#forEach post.content}}
    <section class="loaded" section-type="{{{type}}}" {{#if id}}content-id="{{{id}}}"{{/if}}>{{{data}}}</section>
{{/forEach}}{{/if}}
</article>

<form action="" id="section-select">
    <select name="section-type" id="s-sect-type">
        <option value="text">Text</option>
        <option value="img">Image</option>
    </select>
    <button type="submit" id="s-create">Create</button>
</form>

<button type="submit" id="post-submit">Update Post</button>

<article id="post-details">
    <h3>Post Details</h3>
    <table>
        <colgroup>
            <col style="font-weight: bold;">
            <col span="2">
        </colgroup>
        <tr>
            <th>Property</th>
            <th>Value</th>
        </tr>
        <tr>
            <td>ID</td>
            <td>{{post.id}}</td>
        </tr>
        <tr>
            <td>Title</td>
            <td>{{post.title}} <button>Edit</button></td>
        </tr>
        <tr>
            <td>Slug</td>
            <td>{{post.slug}}</td>
        </tr>
        <tr>
            <td>Description</td>
            <td>{{post.description}} <button>Edit</button></td>
        </tr>
        <tr>
            <td>Thumbnail</td>
            <td><a href="{{post.thumbnail.url}}">View</a> <button>Edit</button></td>
        </tr>
        <tr>
            <td>Created At</td>
            <td>{{date post.created_at "DD MMMM YYYY HH:MM"}}</td>
        </tr>
        <tr>
            <td>Last Modified</td>
            <td>{{date post.edited_at "DD MMMM YYYY HH:MM"}}</td>
        </tr>
    </table>
</article>